# OOAD Assignment

## Running the Application Locally
The JavaFX GUI can be run on a local machine using:
    mvn clean javafx:run

## Running in GitHub Codespaces
Codespaces does not support JavaFX GUI.  
However, the project compiles and all backend logic can run using:
    mvn clean install
